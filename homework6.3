#include <iostream>
#include <string>

using namespace std;

int main ()
{
    string s;

    cout << "Введите строку: ";
    getline(cin, s);

    if (s.empty()) {
        cout << "Строка пуста";
    } else {
        cout << "Первый символ " << s[0];
    }

    return 0;
}
